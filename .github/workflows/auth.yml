name: Auth WF

on:
  workflow_call:
    secrets:
      AZURE_CLIENT_ID:
        required: true
      AZURE_TENANT_ID:
        required: true
      AZURE_SUBSCRIPTION_ID:
        required: true

jobs:

  Auth-Hello:

    # Note that setting this (Copilots idea) doesn't result in any changes to the subject  
    # env:
    #   OIDC_SUBJECT: "repo:${{ github.repository }}:workflow:build"
      
    runs-on: ubuntu-latest
    steps:
      - run: echo Hello from the Auth Workflow.

      - name: Get repository Name
        run: echo "REPO_NAME=${{ github.repository }}" >> $GITHUB_ENV

      - name: Extract first 4 characters
        run: echo "REPO_OWNER=${REPO_NAME:0:4}" >> $GITHUB_ENV

      - name: OIDC Login
       
        # Note that setting this (Copilots idea) doesn't result in any changes to the subject either
        # env:
        #   OIDC_SUBJECT: "repo:${{ github.repository }}:workflow:build"
          
        uses: azure/login@v1
        with:
          audience: api://AzureADTokenExchange-${REPO_OWNER}
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }} 

      - name: 'Run Azure CLI command'
        run: |
          az account show
          pwd
